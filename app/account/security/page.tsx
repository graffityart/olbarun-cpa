import { redirect } from "next/navigation";import DashboardShell from "@/components/DashboardShell";import SecuritySettings from "@/components/SecuritySettings";import { getCurrentUser } from "@/lib/auth/session";
export const dynamic="force-dynamic";export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/login");const home=user.role==="PARTNER"?"/partner":user.role==="ADVERTISER"?"/advertiser":"/admin";return <DashboardShell title="계정 보안" description="비밀번호와 로그인 세션을 관리합니다." nav={[{href:home,label:"← 대시보드"},{href:"/account/security",label:"계정 보안"}]}><section className="panel card" style={{marginBottom:18}}><strong>{user.email}</strong><p className="muted">권한: {user.role}</p></section><SecuritySettings/></DashboardShell>}
