import { eq } from "drizzle-orm";import { getDb } from "@/db";import { advertiserUsers,advertisers,users } from "@/db/schema";import { hashPassword } from "@/lib/auth/password";import { requireAdmin } from "@/lib/auth/guards";import { writeAudit } from "@/lib/audit";import { requireSameOrigin } from "@/lib/security/origin";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{requireSameOrigin(request);const admin=await requireAdmin();const{id}=await params;const body=await request.json();const email=String(body.email??"").trim().toLowerCase();const password=String(body.password??"");if(!email.includes("@")||password.length<10||password.length>128)return Response.json({ok:false,error:"INVALID_INPUT"},{status:400});const db=getDb();const[advertiser]=await db.select({id:advertisers.id,companyName:advertisers.companyName}).from(advertisers).where(eq(advertisers.id,id)).limit(1);if(!advertiser)return Response.json({ok:false,error:"ADVERTISER_NOT_FOUND"},{status:404});const linked=await db.select({id:advertiserUsers.id}).from(advertiserUsers).where(eq(advertiserUsers.advertiserId,id)).limit(1);if(linked.length)return Response.json({ok:false,error:"ACCOUNT_ALREADY_EXISTS"},{status:409});const existing=await db.select({id:users.id}).from(users).where(eq(users.email,email)).limit(1);if(existing.length)return Response.json({ok:false,error:"EMAIL_ALREADY_EXISTS"},{status:409});const result=await db.transaction(async tx=>{const[user]=await tx.insert(users).values({email,passwordHash:hashPassword(password),role:"ADVERTISER",status:"ACTIVE"}).returning();await tx.insert(advertiserUsers).values({advertiserId:id,userId:user.id,role:"OWNER"});return user;});await writeAudit({actorUserId:admin.id,action:"ADVERTISER_ACCOUNT_CREATED",targetType:"ADVERTISER",targetId:id,summary:`${advertiser.companyName} 광고주 로그인 계정 생성`,metadata:{userId:result.id,email}});return Response.json({ok:true,userId:result.id},{status:201});}catch(error){const code=error instanceof Error?error.message:"CREATE_FAILED";const status=code==="INVALID_ORIGIN"||code==="ORIGIN_CHECK_FAILED"?403:500;return Response.json({ok:false,error:code},{status});}}
