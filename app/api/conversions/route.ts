import { createHash, randomBytes } from "node:crypto";
import { and, eq, gte, sql } from "drizzle-orm";
import { cookies } from "next/headers";
import { getDb } from "@/db";
import { advertiserLedger, campaignRates, campaigns, clicks, conversionData, conversions } from "@/db/schema";
import { notifyAdmins, notifyAdvertiser } from "@/lib/notifications";
const hash=(v:string)=>createHash("sha256").update(v).digest("hex"); const normalizePhone=(v:string)=>v.replace(/\D/g,"");
export async function POST(request:Request){try{const body=await request.json();const campaignId=String(body.campaignId??"");const name=String(body.name??"").trim();const phone=normalizePhone(String(body.phone??""));if(!campaignId||!name||!/^01\d{8,9}$/.test(phone))return Response.json({ok:false,error:"INVALID_INPUT"},{status:400});if(!body.privacy||!body.thirdParty)return Response.json({ok:false,error:"CONSENT_REQUIRED"},{status:400});const db=getDb();const[campaign]=await db.select().from(campaigns).where(and(eq(campaigns.id,campaignId),eq(campaigns.type,"CPA"))).limit(1);if(!campaign)return Response.json({ok:false,error:"CAMPAIGN_NOT_FOUND"},{status:404});if(campaign.status!=="ACTIVE")return Response.json({ok:false,error:"CAMPAIGN_NOT_ACTIVE"},{status:409});const[rate]=await db.select().from(campaignRates).where(eq(campaignRates.campaignId,campaignId)).limit(1);if(!rate)return Response.json({ok:false,error:"RATE_NOT_CONFIGURED"},{status:409});const[balanceRow]=await db.select({balance:sql<number>`coalesce(sum(${advertiserLedger.amount}),0)`}).from(advertiserLedger).where(eq(advertiserLedger.advertiserId,campaign.advertiserId));const balance=Number(balanceRow?.balance??0);if(balance<rate.advertiserRate){await db.update(campaigns).set({status:"PAUSED",updatedAt:new Date()}).where(eq(campaigns.id,campaignId));await Promise.all([notifyAdvertiser(campaign.advertiserId,{type:"BALANCE_LOW",title:"예치금 부족으로 캠페인이 중지되었습니다",message:`${campaign.name} 캠페인의 예치금이 건당 광고비보다 부족해 자동 중지되었습니다.`,href:"/advertiser/ledger"}),notifyAdmins({type:"BALANCE_LOW",title:"광고주 예치금 부족",message:`${campaign.name} 캠페인이 예치금 부족으로 자동 중지되었습니다.`,href:"/admin/advertisers"})]);return Response.json({ok:false,error:"ADVERTISER_BALANCE_INSUFFICIENT"},{status:409});}const phoneHash=hash(phone);const duplicateSince=new Date(Date.now()-campaign.duplicateDays*86400000);const duplicates=await db.select({id:conversionData.id}).from(conversionData).innerJoin(conversions,eq(conversionData.conversionId,conversions.id)).where(and(eq(conversions.campaignId,campaignId),eq(conversionData.customerPhoneHash,phoneHash),gte(conversions.submittedAt,duplicateSince))).limit(1);if(duplicates.length)return Response.json({ok:false,error:"DUPLICATE"},{status:409});const cookieStore=await cookies();const clickCode=cookieStore.get("olbarun_click")?.value??null;let click:{id:string;partnerId:string;trackingLinkId:string}|undefined;if(clickCode)[click]=await db.select({id:clicks.id,partnerId:clicks.partnerId,trackingLinkId:clicks.trackingLinkId}).from(clicks).where(and(eq(clicks.clickCode,clickCode),eq(clicks.campaignId,campaignId))).limit(1);const conversionCode=`CV-${randomBytes(7).toString("hex").toUpperCase()}`;const margin=rate.advertiserRate-rate.partnerBaseRate;const[conversion]=await db.insert(conversions).values({conversionCode,campaignId,partnerId:click?.partnerId??null,trackingLinkId:click?.trackingLinkId??null,clickId:click?.id??null,status:"RECEIVED",advertiserRateSnapshot:rate.advertiserRate,partnerRateSnapshot:rate.partnerBaseRate,platformMarginSnapshot:margin,duplicateStatus:"NORMAL",fraudStatus:"NORMAL"}).returning();await db.insert(conversionData).values({conversionId:conversion.id,customerName:name,customerPhoneEncrypted:phone,customerPhoneHash:phoneHash,region:String(body.region??"").trim()||null,formData:{},privacyPolicyVersion:"privacy-v1",thirdPartyPolicyVersion:"third-party-v1",privacyAgreedAt:new Date(),thirdPartyAgreedAt:new Date()});await notifyAdvertiser(campaign.advertiserId,{type:"NEW_CPA_DB",title:"새 CPA DB가 접수되었습니다",message:`${campaign.name} 캠페인에 ${conversionCode} DB가 새로 접수되었습니다.`,href:"/advertiser/conversions"});return Response.json({ok:true,conversionCode},{status:201});}catch(error){console.error("Conversion create failed",error);return Response.json({ok:false,error:"CREATE_FAILED"},{status:500});}}
