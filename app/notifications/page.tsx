import { desc, eq } from "drizzle-orm";import { redirect } from "next/navigation";import DashboardShell from "@/components/DashboardShell";import ReadAllNotifications from "@/components/ReadAllNotifications";import { getDb } from "@/db";import { notifications } from "@/db/schema";import { getCurrentUser } from "@/lib/auth/session";
export const dynamic="force-dynamic";export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/login");const rows=await getDb().select().from(notifications).where(eq(notifications.userId,user.id)).orderBy(desc(notifications.createdAt)).limit(100);const unread=rows.filter(n=>!n.isRead).length;const home=user.role==="PARTNER"?"/partner":user.role==="ADVERTISER"?"/advertiser":"/admin";return <DashboardShell title="알림센터" description={`읽지 않은 알림 ${unread}건`} nav={[{href:home,label:"← 대시보드"},{href:"/notifications",label:"알림센터"}]}><div className="page-toolbar"><div className="muted">최근 알림 {rows.length}건</div>{unread>0&&<ReadAllNotifications/>}</div><section className="panel card"><div className="notification-list">{rows.length?rows.map(n=><article key={n.id} className={`notification-item ${n.isRead?"":"unread"}`}><div><span className="badge">{n.type}</span>{!n.isRead&&<span className="unread-dot">NEW</span>}<h3>{n.title}</h3><p>{n.message}</p><small className="muted">{new Date(n.createdAt).toLocaleString("ko-KR")}</small></div><div>{n.href&&<a href={`/api/notifications/${n.id}/read?next=${encodeURIComponent(n.href)}`}>확인 →</a>}</div></article>):<p className="empty-cell">새로운 알림이 없습니다.</p>}</div></section></DashboardShell>}
